version: '3.8'
services:
  php:
    build:
      context: ./php
    container_name: ${COMPOSE_PROJECT_NAME}_php
    environment:
      - PROJECT_NAME=${PROJECT_NAME}
      - XDEBUG=${XDEBUG:-false}
    volumes:
      - ./src:/var/www/html
    networks:
      - obe_network
    depends_on:
      db:
        condition: service_healthy

  nginx:
    build:
      context: ./nginx
    container_name: ${COMPOSE_PROJECT_NAME}_nginx
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - ./src:/var/www/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./nginx/ssl:/etc/nginx/ssl:ro
    networks:
      - obe_network
    depends_on:
      - php

  db:
    image: mariadb:10.11
    container_name: ${COMPOSE_PROJECT_NAME}_db
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    ports:
      - "13306:3306"
    environment:
      MYSQL_DATABASE: ${PROJECT_NAME}
      MYSQL_ROOT_PASSWORD: p455w0rd
    volumes:
      - ./db/conf.d:/etc/mysql/conf.d
      - ./db/data:/var/lib/mysql
    networks:
      - obe_network

  minio:
    image: minio/minio:latest
    container_name: ${COMPOSE_PROJECT_NAME}_minio
    restart: unless-stopped
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - ./minio/data:/data
    command: server /data --console-address ":9001"
    networks:
      - obe_network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  metabase:
    image: metabase/metabase:latest
    container_name: ${COMPOSE_PROJECT_NAME}_metabase
    hostname: metabase
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      MB_DB_TYPE: h2
      MB_DB_FILE: /metabase-data/metabase.db
      MB_SITE_NAME: "APP - ${PROJECT_NAME}"
      MB_EMOJI_IN_LOGO: false
    volumes:
      - ./metabase/data:/metabase-data
    networks:
      - obe_network
    depends_on:
      - db
    healthcheck:
      test: curl --fail -I http://localhost:3000/api/health || exit 1
      interval: 15s
      timeout: 5s
      retries: 10

  # n8n:
  #   image: n8nio/n8n:latest
  #   container_name: ${COMPOSE_PROJECT_NAME}_n8n
  #   hostname: n8n
  #   restart: unless-stopped
  #   user: "1000:1000"
  #   ports:
  #     - "5678:5678"
  #   environment:
  #     - N8N_HOST=${N8N_HOST:-localhost}
  #     - N8N_PORT=5678
  #     - N8N_PROTOCOL=http
  #     - WEBHOOK_URL=http://localhost:5678/
  #     - GENERIC_TIMEZONE=Asia/Jakarta
  #     - N8N_ENCRYPTION_KEY=${N8N_ENCRYPTION_KEY:-n8n-encryption-key-change-this}
  #     - N8N_LOG_LEVEL=info
  #   volumes:
  #     - ./n8n/data:/home/node/.n8n
  #   networks:
  #     - obe_network
  #   healthcheck:
  #     test: ["CMD", "wget", "--spider", "-q", "http://localhost:5678/healthz"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3

  # waha:
  #   image: devlikeapro/waha:latest
  #   container_name: ${COMPOSE_PROJECT_NAME}_waha
  #   hostname: waha
  #   restart: unless-stopped
  #   ports:
  #     - "3001:3000"
  #   environment:
  #     - WHATSAPP_HOOK_URL=http://n8n:5678/webhook/whatsapp
  #     - WHATSAPP_HOOK_EVENTS=message,session.status
  #     - WAHA_API_KEY=${WAHA_API_KEY:-waha-secret-key-change-this}
  #     - WAHA_LOG_LEVEL=info
  #     - WAHA_DASHBOARD_ENABLED=true
  #     - WAHA_DASHBOARD_USERNAME=${WAHA_USERNAME:-admin}
  #     - WAHA_DASHBOARD_PASSWORD=${WAHA_PASSWORD:-admin123}
  #   volumes:
  #     - ./waha/data:/app/.wwebjs_auth
  #     - ./waha/files:/app/files
  #   networks:
  #     - obe_network
  #   depends_on:
  #     - n8n

networks:
  obe_network:
    driver: bridge